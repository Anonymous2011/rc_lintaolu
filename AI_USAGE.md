# AI 使用说明

这个项目从需求分析、方案设计到代码实现，全程使用了 Claude（Claude Code）。
下面记录 AI 在哪里真正帮上了忙、哪些建议我没有采纳，
以及哪些决定是我自己做的。

---

## 一、AI 在哪些关键地方提供了帮助

**代码实现。**  几乎100%代码的实现以及数据库设计。

**失败分类的具体划分。** 哪些该重试、哪些该立刻停，它给出了完整的分类：5xx 重试、
其他 4xx 终止、408/超时重试、3xx 终止、429 遵守 `Retry-After`。这部分是我自己容易
想漏的（比如 3xx 不跟随、DNS 失败该归为暂时性问题）。

**验证。** 每写完一个失败路径，它都会真的构造一次请求跑通：重试后成功、超时、
被限流后遵守 `Retry-After`、重试耗尽进死信、幂等键重复提交。性能数字也是它用真实表结构
实测出来的，不是估的。

**文档和界面文案。** README 和三个页面的中英文文案基本都是它写的，我主要在做删减和
口语化。

---

## 二、AI 给出过、但我没有采纳的建议

**熔断器。** 它一开始建议加，理由是供应商连续失败时应该快速失败。我没采纳：
单供应商并发上限加上间隔递增已经能防止一家拖垮其他，熔断器只会多一套状态、
多一条半开探测路径、多一类新的出错方式。这条后来写进了「觉得没必要做的」。
（有意思的是，它在实现完其余部分后自己也改口不建议做了。）

**过于复杂的UI设计。** AI主动增加了大量无用的UI功能，这个项目中，UI仅仅是辅助，不是核心功能。我命令AI删除冗余设计，只留下主要的功能。

---

## 三、哪些关键决策是我自己做的

**产品边界。** 我读了文档需求，确立了产品的边界。那些是需要这个系统解决的，那些事这个系统不需要解决的。AI做了一些细节的补充。

**产品架构。**  我经过分析认为产品应该包括API服务，存储，重试逻辑，以及可视化几个方面。

**做成一个网页。** 需求要求 MVP，所以我决定做成一个网页，因为它同时具备API接口以及可视化服务，方便验收。运行起来非常也方便。

**使用原生HTTP请求。** 对上游只暴露一个普通的 HTTP 接口，不提供 SDK、不用 gRPC。
各个业务系统的语言和框架都不一样，HTTP 是所有人都能直接调的最小公约数，接入方不用
装任何东西。

**限流由上游控制，这个服务不做主动限流。** 我的判断是：为每家供应商存一份配额配置，
等于又一份供应商定制知识，和我们拒绝掉的「报文模板」是同一类错误。

**文档结构。** 我提出文档的大致框架，AI根据我的框架补足文档内容。
